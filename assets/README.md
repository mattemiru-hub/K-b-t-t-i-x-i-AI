# Assets

This folder is reserved for reference assets used by the skill.

In the local working copy, it includes:

- `golden-reference-preview.png`: preview image of the approved dashboard layout

When the skill is published through text-only GitHub connector flows, the binary preview image may need to be uploaded separately.
